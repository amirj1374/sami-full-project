# Run P3-S7-001 — Reservation Timeout

- **Run ID:** `RUN-P3-S7-001`
- **Contract ID:** `CONTRACT-P3-S7-RESERVATION-TIMEOUT`
- **Authorization ID:** `AUTH-P3-S7-RESERVATION-TIMEOUT`
- **Status:** COMPLETED
- **Started From:** Accepted Phase 3 Step 6 baseline on `development`.

## Agents / Responsibilities

- Lead / Orchestrator: scope, integration, validation and state closure.
- Backend: timeout configuration and reservation expiry service path.
- Data & Integrity: PostgreSQL persistence and idempotent release semantics.
- QA & Scenario: acceptance and regression coverage.
- Guardian: independent plan and implementation conformance review.

## Dependency Plan

P3-S5 reservation/backorder foundations → P3-S7 timeout/expiry → P3-S8
cross-module integration. No parallel write work was required; validation ran
after implementation.

## Work / Handoffs

- Added `InventoryProperties` with configurable `app.inventory.reservation-timeout`
  and the approved 30-minute default.
- Persisted `expires_at` on both reservation allocation paths.
- Added tenant-safe, row-locked expiry processing that restores reserved
  projection once, returns serialized units to AVAILABLE, records a RELEASE
  movement, and is idempotent.
- Added permanent PostgreSQL timeout acceptance coverage.

## Failures and Corrections

- F-001 (IMPLEMENTATION/COMPILE): Instant arithmetic/API mismatch in the new
  acceptance test; corrected to `Duration` arithmetic.
- F-002 (DATA/INTEGRATION): PostgreSQL could not infer `Instant` parameter type;
  expiry query binding changed to `Timestamp`.
- F-003 (DATA/INTEGRATION): `EXPIRE` was not an allowed movement type; used the
  existing `RELEASE` movement contract with timeout reason and operation key.
- F-004 (TEST/FIXTURE): tenant-wide expiry can process multiple due reservations
  in one suite; assertion now verifies one-or-more expiry plus the dedicated
  reservation's exact persisted outcome.

## Validation Evidence

- `mvn -B -DskipTests test-compile`: PASS.
- Fresh external PostgreSQL 16.15, Flyway V1→V71: PASS (71 migrations).
- Step 4 + Step 5 acceptance on fresh V71 database: 14 tests, 0 failures,
  0 errors (Step 4: 1; Step 5: 13 including timeout).
- Full backend `mvn -B test`: 310 tests, 0 failures, 0 errors, 0 skipped.
- Final fresh external PostgreSQL 16.15 combined acceptance: 14 tests, 0
  failures, 0 errors (Step 4: 1; Step 5: 13).

## Guardian Result

- Plan review: PASS — P3-S7 traces to BR-INV-009 and the approved roadmap,
  does not invent business behavior, and leaves later work sequenced.
- Implementation review: PASS — timeout persistence, tenant/row locking,
  idempotency, existing movement/status contracts, regression evidence and
  scope boundaries conform to the Contract; no Critical/Major findings.

## Completion Result

P3-S7 DoD satisfied. Project state updated; scoped changes committed and
pushed. P3-S8 remains the next candidate and is not authorized by this run.
