# RUN-P4-S3-001 — Journal/GL Foundation

- **Contract:** `AUTH-P4-S3-JOURNAL-GL-FOUNDATION`
- **Status:** COMPLETED — Guardian PASS
- **Started From:** current Phase 4 accepted P4-S2 foundation
- **Handoff:** Data & Integrity supplied `V74__accounting_journal_gl_foundation.sql`.
- **Implementation:** Adds tenant/company/branch-scoped immutable journal
  headers and lines, scoped posting/idempotency uniqueness, reversal/correction
  provenance, and deferred non-zero balance enforcement.
- **Validation:** `git diff --check` PASS. V74 SQL applied with
  `ON_ERROR_STOP=1` to disposable PostgreSQL 16 database `sami_p4s2_v73`;
  all tables, indexes, functions and triggers created successfully.
- **Scope Note:** Account/tenant/company/branch consistency beyond existing
  single-column foreign keys remains a service validation responsibility; no
  unsafe composite foreign key was introduced.
- **Next Action:** Add/execute posting-service and PostgreSQL acceptance tests,
  then independently review and close P4-S3.

## Completion Update

- **Backend:** Added `JournalPostingPort` and transactional
  `JournalPostingService` for scoped, idempotent, balanced IRR journal posting.
  The service locks the target fiscal period, validates postable active accounts,
  and preserves database-enforced immutable/balanced journal invariants.
- **Compile:** Docker Maven `mvn -B -DskipTests test-compile` PASS.
- **Fresh migration:** PostgreSQL 16.15 disposable empty database
  `sami_p4s3_fresh`; Flyway validated 74 migrations and applied V1→V74 PASS.
- **Supported upgrade:** PostgreSQL 16.15 disposable V72 fixture
  `sami_p4s3_upgrade`; Flyway validated 74 migrations and applied V73→V74 PASS.
- **Runtime note:** the non-web `java -jar` probe exits later on the known
  `MvcRequestMatcher` web-context requirement; this occurs after Flyway
  completion and is not a migration failure.
- **UI Quality:** NOT APPLICABLE — P4-S3 is backend/data-only and adds no UI.
- **Guardian:** PASS; scope is additive, authority-safe, and no Phase 5,
  provider, tax-rate, or UI behavior was invented.
- **DoD:** Satisfied for the P4-S3 foundation slice; next authorized step is
  P4-S4 AR/AP integration.
- **Full backend regression:** 310 tests, 0 failures, 1 error. The sole error
  is the known backend-only environment contract
  `DevelopmentConfigurationContractTest.developmentCorsOriginMatchesVitePort`
  because `../sami-frontend/vite.config.ts` is unavailable inside the Maven
  container. No P4-S3 test or production regression failed.
