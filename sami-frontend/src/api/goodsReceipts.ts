import { http, unwrap } from './http'
export const goodsReceiptsApi={list:()=>unwrap<any[]>(http.get('/v1/goods-receipts')),get:(id:number)=>unwrap<any>(http.get(`/v1/goods-receipts/${id}`)),create:(p:any)=>unwrap<any>(http.post('/v1/goods-receipts',{lines:p.lines,notes:p.notes},{params:{companyId:p.companyId,branchId:p.branchId,purchaseOrderId:p.purchaseOrderId,warehouseId:p.warehouseId},headers:{'Content-Type':'application/json'}}))}
