import test from 'node:test'; import assert from 'node:assert/strict'; import { readFileSync } from 'node:fs'; import { join } from 'node:path';
const root=process.cwd(); const read=p=>readFileSync(join(root,p),'utf8');
test('sales order contract exposes direct creation, quotation conversion, confirmation, correction and stock visibility',()=>{const api=read('src/api/salesOrders.ts'),view=read('src/components/SalesOrdersPanel.vue');assert.match(api,/from-quotation/);assert.match(api,/\/confirm/);assert.match(api,/\/audit/);assert.match(api,/companyId,branchId/);assert.match(view,/reservedQuantity/);assert.match(view,/backorderedQuantity/);assert.match(view,/SalesOrderPayload/);assert.match(view,/salesOrdersApi\.convert/);assert.match(view,/audits/);assert.match(view,/open\(selected\)/);});
